version: '3'

services:
  db:
    container_name: graph_nest_db
    image: postgres:9.6
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: xintru_gql_proj